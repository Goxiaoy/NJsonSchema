{% if HasDescription -%}
///{{ Description | csharpdocs }}
{% endif -%}
{% if IsEnumAsBitFlags -%}
[System.Flags]
{% endif -%}
enum {{ TypeAccessModifier }}{{ Name }}
{
{% for enum in Enums -%}
    {{ enum.Value}},
{% endfor -%}
}