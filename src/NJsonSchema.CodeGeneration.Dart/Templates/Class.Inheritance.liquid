{% if RenderInpc -%}
{% if HasInheritance %} extends {{ BaseClassName }} with ChangeNotifier{% else %}with ChangeNotifier{% endif %}
{% else -%}
{% if HasInheritance %} extends {{ BaseClassName }}{% endif %}
{% endif -%}